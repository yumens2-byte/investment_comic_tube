# Runtime: dry-run | review | publish
APP_MODE=dry-run
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_MAX_BYTES=5242880
LOG_BACKUP_COUNT=3
DATABASE_URL=sqlite:///data/app.db
ARTIFACT_DIR=artifacts

# Supabase REST persistence (optional pilot backend).
# Use only in a trusted server/CI environment; never expose a service-role key.
SUPABASE_URL=
SUPABASE_KEY=

# Market data
DATA_PROVIDER=yfinance
MARKET_TIMEZONE=America/New_York

# Gemini (not wired in the current prototype)
GEMINI_API_KEY=
GEMINI_MODEL=

# YouTube OAuth. Keep production values in a secret manager, never in Git.
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REFRESH_TOKEN=
YOUTUBE_DEFAULT_PRIVACY=private
YOUTUBE_CATEGORY_ID=27
